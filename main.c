#include "header.h"

int game_over = 0;

// update();

int main() {

    int board = generate_map();

    while (!game_over) {
        char c = input();
        // usleep(300000);
    }

    return 0;
}